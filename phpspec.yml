suites:
    main:
        spec_path: tests
        namespace: Cerbero\CommandValidator
        psr4_prefix: Cerbero\CommandValidator
extensions:
    - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
    format:
      - clover
    output:
      clover: coverage.clover
