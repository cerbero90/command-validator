default:
    autoload:
        '': %paths.base%/tests/features/bootstrap
    suites:
        console:
            paths: [ %paths.base%/tests/features ]
    extensions:
        Laracasts\Behat:
            bootstrap_path: laravel/bootstrap/app.php
        Behat\MinkExtension:
            default_session: laravel
            laravel: ~
